#ifndef SR_CONFIG
#define SR_CONFIG

#include <iostream>
#include <glm/glm.hpp>

namespace Config {
    namespace Window {
        extern std::string title;
        extern unsigned int height;
        extern unsigned int width;
        extern float version;
    };
};

#endif
